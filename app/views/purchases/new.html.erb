<nav class="relative flex flex-wrap items-center justify-between w-full py-4 shadow-lg bg-main">
  <div class="flex flex-wrap items-center justify-center w-full px-6 container-fluid">
    <div class="space-x-9 container-fluid">
      <a class="mt-2 -mx-7 lg:mt-0" href="#">
      <%= link_to '←', user_path(current_user.id), class:'text-light text-decoration-none text-xl hover:text-text focus:text-text' %>
        <span class="font-medium text-light">NEW PURCHASE</span>
        <span class="font-medium"><%= link_to 'Next',{}, class: 'text-light text-m text-decoration-none hover:text-text focus:text-text'%></span>
      </a>
    </div>
  </div>
</nav>

<div class= 'flex flex-col items-center justify-center mt-4 space-y-6'>
  <div class='w-70'>
  <%=form_with model: @purchase, url: user_group_purchases_path, scope: :purchase do |form| %>
  <%= form.label 'Purchase name', class: 'text-text font-medium' %>
  <%= form.text_field :name, class:'px-4 flex items-center w-72 rounded  transition duration-300 ease-in-out text-heading text-base focus:outline-none focus:ring-0 border border-border-base focus:border-accent h-10 my-4', require: true %>
  <%= form.label :amount, class: 'text-text font-medium' %>
  <%= form.text_field :amount, class:'px-4 flex items-center w-72 rounded  transition duration-300 ease-in-out text-heading text-base focus:outline-none focus:ring-0 border border-border-base focus:border-accent h-10 my-4', require: true %>
</div>


<div class= 'h-12 p-4 my-4 font-semibold leading-none text-white transition duration-300 ease-in-out border border-transparent rounded outline-none cursor-pointer -ml-36 bg-main max-w-fit focus:outline-none focus:shadow focus:ring-1 focus:ring-accent-700 bg-accent hover:bg-primary'>
  <%= form.submit 'Add Purchase', class: 'cursor-pointer' %>
</div>

<% end %>
</div>